export default class ConsumetClient {
    public static readonly baseApiUrl = "http://localhost:3000/";

    public static GetEpisodeSource(
        aniListId: string,
        episodeProgress: string,
        episodeProvider: string = "Zoro",
    ) {}
}
